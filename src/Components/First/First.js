import React, { useState } from "react";
import Card from "./Card";
import "./First.scss";
const First = () => {
  return (
    <section id="first" className="container">
      <Card />
    </section>
  );
};

export default First;
